# P.275

from math import comb


N, A, B = 11, 25, 24


if __name__ == '__main__':
    # 10点で同点になるまでのパターン数。20回の得点機会の内、片方が10点を取る場合の数。
    for_10 = comb(20, 10)
    # 11～24点はどちらかが点数をとると、次はもう片方が点数をとる
    for_24 = 2 ** (B - N + 1)
    print(for_10 * for_24)
